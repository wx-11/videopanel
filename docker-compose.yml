services:
  sora2-manager:
    build:
      context: .
      args:
        VITE_AUTH_ENABLED: ${VITE_AUTH_ENABLED:-false}
        VITE_AUTH_USERNAME: ${VITE_AUTH_USERNAME:-internal}
    ports:
      - "${PORT:-18130}:80"
    restart: unless-stopped
