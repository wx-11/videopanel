## 是否开启网关鉴权（OpenResty Basic Auth）
# - 默认不开启：本地开发/直连 sora2api 更方便
# - 开启后：前端请求会携带 Basic Authorization（用户名为 VITE_AUTH_USERNAME）
#
# 注意：不要把 sora2api 的 API Key 写在前端 .env 里（会打包进浏览器可见的 JS）。
# 推荐做法：由 OpenResty 反代到 sora2api，并在反代里注入 Bearer Key。

VITE_AUTH_ENABLED=false
VITE_AUTH_USERNAME=internal
